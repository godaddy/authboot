{
  "name": "authboot",
  "version": "1.0.0",
  "description": "a simple slay preboot to instantiate an auth middleware and a custom/default lookup function on the app object",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha"
  },
  "keywords": [
    "slay",
    "preboot",
    "express",
    "http"
  ],
  "author": "GoDaddy Engineers",
  "license": "MIT",
  "dependencies": {
    "diagnostics": "^1.1.0",
    "express-basic-auth-safe": "^1.1.4"
  },
  "devDependencies": {
    "assume": "^1.5.2",
    "mocha": "^5.0.4",
    "nyc": "^11.6.0"
  }
}
